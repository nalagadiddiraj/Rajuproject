<!DOCTYPE html>
<html>
<head>
  <title>Your Page Title</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    /* Add your custom CSS styles here */
    body {
      font-family: Arial, sans-serif;
      margin: 0; /* Remove default margin for body */
      padding: 0; /* Remove default padding for body */
      background-image: url("https://plus.unsplash.com/premium_photo-1673529828969-e0ad425308b7?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=870&q=80");
      background-repeat: no-repeat;
      background-attachment: fixed;
      background-position: center;
      background-size: cover; /* Make the background image cover the entire screen */
      height: 100vh; /* Set the body height to 100% of the viewport height */
      display: flex; /* Use flexbox to center content vertically */
      justify-content: center; /* Center content horizontally */
      align-items: center; /* Center content vertically */
    }

    h1 {
      color: #333;
      text-decoration: underline;
    }

    p {
      color: #666;
      line-height: 1.6;
    }

    #notice {
      font-weight: bold;
      color: red;
    }

    /* Additional custom styles for sign-in/log-in links */
    .user-section {
      background-color: rgba(255, 255, 255, 0.8); /* Add a semi-transparent background for better readability */
      padding: 20px;
    }

    .signup-link,
    .login-link {
      display: block;
      margin: 10px auto;
      max-width: 200px; /* Limit the width of the sign-in/log-in links */
    }
  </style>
</head>
<body>
  <!-- Your content goes here -->
  <div class="user-section">
  <h1>  Create Login </h1>
    <% if current_user %>
      Logged in as <span class="user-email"><%= current_user.email %></span>.
      <%= link_to 'Log Out', logout_path, class: "logout-link" %>
    <% else %>
      <%= link_to 'Sign Up', signup_path, class: "signup-link btn btn-primary" %>
      <%= link_to 'Log In', login_path, class: "login-link btn btn-secondary" %>
    <% end %>
  </div>
  
  <!-- Link to Bootstrap JS and jQuery (optional, if you need Bootstrap JS functionality) -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
